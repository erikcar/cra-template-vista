//import { DataGraph } from "@vista/core/DataGraph"

//import { DataGraph } from "@vista/react";

export function AppSchema(){
    //console.log(DataGraph.getESchema("prova"));
    /*DataGraph.setSchema(`folder {*,
        patient: /u patient {*},
        visits: [visit] {id, dstart, dend, iddoctor,
            exams: [exam] {*}
        }
    }`, 'folder'); //ADD Link definition (optional)*/
}